\section{Methodology}

In this section, we explain the methodology used to conduct our research. We first describe the dataset and preprocessing pipeline. Next, we outline the traditional machine learning models and LLMs we employed for our experiments. Finally, we discuss the evaluation metrics and experimental setup.

\subsection{Dataset and Preprocessing}

We utilized a dataset named Open University Learning Analytics dataset (hereby referred to as OULAD), described in \cite{Kuzilek2017}. This dataset reports the background, behavior, and performance of students from England in a given Virtual Learning Environment (VLE). The dataset contains 32,593 records of 20 features, including demographic information, course interactions, and final results. The features include both categorical and numerical data, which required preprocessing before model training. The dataset, as outlined by its authors, consists of four modules:

\begin{description}
    \item[Student Demographics:] This module includes background information about the students, such as age, gender, and educational as well as socio-economic background. One notable feature is the Index of multiple deprivation (\href{https://www.gov.uk/government/statistics/english-indices-of-deprivation-2025/english-indices-of-deprivation-2025-statistical-release}{IMD}), which is a measure of socio-economic status based on the students' postal codes.
    \item[Course Registrations:] This module contains information about the courses students are registered for, including course/presentation codes and their lengths. It also includes data students' registration and  unregistration dates.
    \item[Assessment Results:] This module captures the students' performance in three types of assessments, namely Tutor Marked Assessments (TMA), Computer Marked Assessments (CMA), and Final Exams (Exam), adjusted by weights. It provides insights into their academic progress and outcomes.
    \item[VLE interactions:] This module records the students' interactions with the Virtual Learning Environment (VLE), including the number of clicks on various pages and resources. It also includes the dates of these interactions, which can be used to analyze engagement patterns over time. This module also provides information on the resources in the VLE.
\end{description}

The tables outlining these modules are given as follows:

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \renewcommand{\arraystretch}{1.18}
    \begin{tabularx}{\linewidth}{|l|l|c|X|}
    \hline
    \textbf{Module} & \textbf{Table} & \textbf{Records} & \textbf{What it contains (main fields)} \\
    \hline
    Student Demographics & \texttt{studentInfo} & 32,593 & Student profile and outcome labels: \texttt{id\_student}, demographics (gender, region, education, disability), socio-economic and age bands, prior attempts, studied credits, and \texttt{final\_result}. \\
    \hline
    Course Registrations & \texttt{courses} & 22 & Module-presentation metadata: \texttt{code\_module}, \texttt{code\_presentation}, and course \texttt{length}. \\
    \hline
    Registrations & \texttt{studentRegistration} & 32,593 & Enrollment timeline per student and presentation: \texttt{id\_student}, \texttt{date\_registration}, and \texttt{date\_unregistration}. \\
    \hline
    \end{tabularx}
    \caption{OULAD: Student and Course Demographics}
    \label{tab:demographics}
\end{table}

\newpage

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \renewcommand{\arraystretch}{1.18}
    \begin{tabularx}{\linewidth}{|l|l|c|X|}
    \hline
    \textbf{Module} & \textbf{Table} & \textbf{Records} & \textbf{What it contains (main fields)} \\ \hline
    Assessment Results & assessments & 206 & Assessment structure: \texttt{code\_module}, \texttt{code\_presentation}, \texttt{id\_student}, \texttt{id\_assessment}, \texttt{assessment\_type}, \texttt{date}, \texttt{weight} \\ \hline
    Assessment Results & studentAssessment & 173,912 & Student performance: \texttt{id\_student}, \texttt{id\_assessment}, \texttt{is\_banked}, \texttt{date\_submitted}, \texttt{score} \\ \hline
    \end{tabularx}
    \caption{OULAD: Assessment Data}
    \label{tab:assessments}
\end{table}

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \renewcommand{\arraystretch}{1.18}
    \begin{tabularx}{\linewidth}{|l|l|c|X|}
    \hline
    \textbf{Module} & \textbf{Table} & \textbf{Records} & \textbf{What it contains (main fields)} \\ \hline
    VLE interactions & interactions & 10,655,280 & Clickstream data: \texttt{code\_module}, \texttt{code\_presentation}, \texttt{id\_student}, \texttt{id\_site}, \texttt{date}, \texttt{sum\_click} \\ \hline
    VLE interactions & vle & 6,364 & VLE resource metadata: \texttt{id\_site}, \texttt{code\_module}, \texttt{code\_presentation}, \texttt{activity\_type}, \texttt{week\_from}, \texttt{week\_to} \\ \hline
    \end{tabularx}
    \caption{OULAD: Virtual Learning Environment Interactions}
    \label{tab:vle}
\end{table}

To correctly and effectively classify the students' final results, preprocessing must be done to merge the tables and create a single table containing all the features, keyed according to the \texttt{id\_student} column. In the next subsection, we describe the preprocessing steps taken to achieve this.

\subsubsection{Preprocessing}
In the figure below, we outline the steps taken to preprocess the OULAD dataset. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/PP_pipeline_LBL1_input_tables.png}
    \caption{Preprocessing pipeline (Stage 1): input tables.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/PP_pipeline_LBL2_feature_preparation.png}
    \caption{Preprocessing pipeline (Stage 2): feature preparation.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/PP_pipeline_LBL3_prepared_feature_sets.png}
    \caption{Preprocessing pipeline (Stage 3): prepared feature sets.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/PP_pipeline_LBL4_final_dataset.png}
    \caption{Preprocessing pipeline (Stage 4): final dataset construction.}
\end{figure}



We divide the tables into three groups: background (studentInfo), performance (studentAssessment and assessments), and behavior (studentVle, vle, and assessments for cutoff construction). We thus perform the following preprocessing tasks on each group as follows:

\begin{description}
    \item[Background:] We first perform one-hot encoding on the categorical features in the \texttt{studentInfo} table, which include students' gender, region, highest education and disability status. For age and IMD bands, we perform ordinal encoding, as these features have an inherent order.
    \item[Performance:] We first merge the \texttt{studentAssessment} and \texttt{assessments} tables on \texttt{id\_assessment}. Missing due dates and scores are explicitly tracked using binary indicators (\texttt{has\_due\_date} and \texttt{score\_missing}), and missing values are imputed using the median. We then engineer timing features from submission behavior, including \texttt{submission\_delay}, late/early flags, and a capped delay variable based on the 1st and 99th percentiles to reduce outlier effects. In addition, we compute weighted performance (\texttt{weighted\_score}) using assessment weights and its min-max normalized version. Finally, we aggregate all assessment records at the student-module-presentation level (\texttt{id\_student}, \texttt{code\_module}, \texttt{code\_presentation}) to produce summary statistics (counts, central tendency, variability, weighted-score totals, lateness ratios, delay average, and missingness ratios), and append assessment-type frequency features via a pivot table.
    \item[Behavior:] To construct behavior features from VLE interactions, we first derive a module-presentation cutoff date as the latest assessment date and keep only clicks that occur on or before this cutoff, preventing post-assessment leakage. We then merge \texttt{studentVle} with \texttt{vle} to attach \texttt{activity\_type} for each \texttt{id\_site} (with missing types labeled as unknown). Next, we aggregate \texttt{sum\_click} values per student-module-presentation and activity type, and pivot them into wide features (e.g., \texttt{clicks\_forum}, \texttt{clicks\_resource}). Because interactions are processed in chunks for scalability, we finally sum partial chunk-level results to obtain one behavior-feature row per (\texttt{id\_student}, \texttt{code\_module}, \texttt{code\_presentation}).
\end{description}

After preprocessing, we aggregate three groups of features, using \texttt{student\_id} as the key, to create the final dataset for modeling. This dataset contains 32,593 records and 72 features, including the students' ID and their final results. The final results are categorized into four classes: \texttt{Distinction}, \texttt{Pass}, \texttt{Fail}, and \texttt{Withdrawn}. 

Next, we split the dataset into training and testing sets, with an 80-20 split. We use \texttt{sk\_learn}'s \texttt{SMOTE} to address class imbalance in the training set, which is a common issue in educational datasets where certain outcomes (e.g., withdrawal) may be more prevalent than others. This  increases the size of the train set to 39,556 records, which raises the total dataset size to 46,076 records.

\subsection{Modeling}

\subsubsection{Traditional Machine Learning Models}

In our paper, we utilize seven traditional machine learning models as baselines for comparison with large language models. These models include: Random Forest (RF), Support Vector Machine (SVM), Gradient Boosting (GB), Decision Tree (DT), K-Nearest Neighbors (KNN), Extreme Learning Machine (ELM), and Multi-layer Perceptron (MLP).

The first two models, RF and SVM, are learning methods identified as top classifiers in Delgado et al.'s systematic review of educational data mining \cite{Delgado2014}. The paper also highlights other strong models, such as DT (C5.0 implementation), GB, ELM, and MLP. A review of the OULAD dataset by Alhakbani and Alnassar \cite{Alhakbani2022} also identifies DT (CART implementation) as a top-performing model in detecting at-risk students. Finally, KNN is also included, due to its simplicity, as we intend to explore if the classification task can be effectively solved using a simple distance-based method.

Regarding implementation, all models are run with \texttt{sk\_learn} libraries; for the DT model, we also uses R programming language to implement the C5.0 algorithm, since \texttt{sk\_learn} only provides the CART implementation. We first run a 5-fold cross-validation on the training set to perform hyperparameter tuning for each model. We then select the best hyperparameters based on the average F1-score across folds, and train the final model on the entire training set using these hyperparameters.

\subsubsection{Large Language Models}

For baseline LLMs, we utilize two models available on Huggin Face: \texttt{Qwen-2.5-3b} and \texttt{LlaMa-3-8b}. The models are not fine-tuned and was prompted to perform the classification task with zero-shot prompting.
\newpage

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.18}
\begin{tabularx}{\textwidth}{|l|X|}
\hline
\textbf{Model} & \textbf{Hyperparameters} \\
\hline
Random Forest & \texttt{n\_estimators=300}, \texttt{min\_samples\_split=5}, \texttt{min\_samples\_leaf=2}, \texttt{max\_features='sqrt'}, \texttt{max\_depth=50}, \texttt{bootstrap=False} \\
\hline
Decision Tree & \texttt{splitter='best'}, \texttt{min\_samples\_split=20}, \texttt{min\_samples\_leaf=4}, \texttt{max\_features=None}, \texttt{max\_depth=10}, \texttt{criterion='gini'} \\
\hline
Support Vector Machine & \texttt{StandardScaler()} + \texttt{SVC(C=0.7847599703514611, class\_weight='balanced', kernel='rbf', gamma='auto')} \\
\hline
Gradient Boosting & \texttt{subsample=1.0}, \texttt{n\_estimators=100}, \texttt{min\_samples\_split=2}, \texttt{min\_samples\_leaf=4}, \texttt{max\_features=None}, \texttt{max\_depth=5}, \texttt{learning\_rate=0.2} \\
\hline
K-NN & \texttt{StandardScaler()} + \texttt{KNeighborsClassifier(n\_neighbors=7, weights='uniform', p=1, metric='minkowski', leaf\_size=50)} \\
\hline
Extreme Learning Machine & \texttt{ELMClassifier(n\_neurons=1000, alpha=0.0001, ufunc='relu')} \\
\hline
Multi-layer Perceptron & \texttt{MLPClassifier(hidden\_layer\_sizes=(32,), activation='tanh', alpha=0.1, solver='lbfgs', max\_iter=1500, max\_fun=15000)} \\
\hline
\end{tabularx}
\caption{Hyperparameters used for each traditional machine learning model.}
\label{tab:model_hyperparameters}
\end{table}

\begin{figure}[H]
	\centering
    \includegraphics[width=0.91\textwidth]{code/plots/random_search_cv_py.png}
    \caption{Cross-validation results for traditional machine learning models.}
\end{figure}

\begin{lstlisting}[style=custompython,caption={Function to build LLMs prompt}]
def build_prompt(
    base_prompt: str,
    row_dict: dict,
    few_shot_examples: list[tuple[dict, str]],
) -> str:
    current_row_json = json.dumps(row_dict, ensure_ascii=False)

    parts = [
        base_prompt,
        "Allowed labels: Distinction, Pass, Withdrawn, Fail.",
        "Infer the label from the pattern in the examples.",
        "Do not default to the most common example label.",
        "Output exactly one line in this format:",
        "LABEL=<one of Distinction, Pass, Withdrawn, Fail>",
        "Do not output anything else.",
    ]

    if few_shot_examples:
        parts.append("")
        parts.append("Examples:")

        for idx, (example_x, example_y) in enumerate(few_shot_examples, start=1):
            example_x_json = json.dumps(sanitize_row(example_x), ensure_ascii=False)
            parts.extend(
                [
                    f"Example {idx}:",
                    f"Student record:\n{example_x_json}",
                    f"LABEL={example_y}",
                    "",
                ]
            )

    parts.extend(
        [
            "Now classify this record:",
            f"Student record:\n{current_row_json}",
            "Answer:",
        ]
    )

    return "\n".join(parts)
\end{lstlisting}

